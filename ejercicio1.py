# Problema 1.

base = float(input(("Ingrese la medida de la base del triangulo (cm): ")))
altura = float(input(("Ingrese la medida de la altura del triangulo (cm): ")))

area = base * altura / 2

print("El area del triangulo es de: %s cm" % area)

